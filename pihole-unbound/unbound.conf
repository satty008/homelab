server:
  # Increase TCP connections to prevent Pi-hole errors
  incoming-num-tcp: 125
  verbosity: 0
  interface: 127.0.0.1
  port: 5335
  do-ip6: no
  do-ip4: yes
  do-udp: yes
  do-tcp: yes
  num-threads: 1

  # Privacy & hardening
  hide-identity: yes
  hide-version: yes
  harden-glue: yes
  harden-dnssec-stripped: yes
  harden-referral-path: yes
  use-caps-for-id: no
  harden-algo-downgrade: no
  qname-minimisation: yes
  aggressive-nsec: yes

  # Performance
  rrset-roundrobin: yes
  cache-min-ttl: 300
  cache-max-ttl: 14400
  msg-cache-slabs: 8
  rrset-cache-slabs: 8
  infra-cache-slabs: 8
  key-cache-slabs: 8
  serve-expired: yes
  serve-expired-ttl: 3600
  edns-buffer-size: 1232
  prefetch: yes
  prefetch-key: yes
  target-fetch-policy: "3 2 1 1 1"
  unwanted-reply-threshold: 10000000
  rrset-cache-size: 256m
  msg-cache-size: 128m
  so-rcvbuf: 1m

  # DNS rebinding protection — block private IP ranges from upstream
  private-address: 192.168.0.0/16
  private-address: 169.254.0.0/16
  private-address: 172.16.0.0/12
  private-address: 10.0.0.0/8
  private-address: fd00::/8
  private-address: fe80::/10

  # Allow local private domains to resolve to private IPs
  private-domain: "bhavibhavan.duckdns.org"
  private-domain: "debugndeadlift.duckdns.org"

  tls-cert-bundle: "/etc/ssl/certs/ca-certificates.crt"

  # DNSSEC validation using root trust anchor
  auto-trust-anchor-file: "/var/lib/unbound/root.key"

remote-control:
  control-enable: yes
  control-interface: /run/unbound.ctl

# True recursive DNS — Unbound queries root servers directly
# No forward-zone block = no upstream provider sees your queries
# To switch to DoT forwarding instead, add a forward-zone block pointing to
# e.g. 8.8.8.8@853#dns.google or 1.1.1.1@853#cloudflare-dns.com